

export default produit = {

    init: (request, response) => {
        // traitements
        response.status.json({
            message: "Ceci est le Model produit"
        })
    },

    // Methodes suivants
}